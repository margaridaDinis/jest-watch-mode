const page11 = document.createElement('template');
page11.innerHTML = `
  <section>
    <p>
      <b>t</b>  is similar to <b>p</b>, but instead of filtering the tests based on the file path, it does it based on the test name.<br>
      It can be used in combination with other options, including <b>p</b>.
    </p>
    <h1>Options <span class="respectCase">t</span></h1>
  </section>
`;

export default page11;
