const page3 = document.createElement('template');
page3.innerHTML = `
  <section>
    <p>In the root of your project, run one this command:</p>
    <pre class="snippet">
      <code>
  $  jest --init
      </code>
    </pre>
    <p>This will prompt few questions and based on your answers create a <em>jest.config.js</em></p>

    <h1>How to <span>configure?</span></h1>
  </section>
`;

export default page3;
