const page4 = document.createElement('template');
page4.innerHTML = `
  <section>
    <p>Create a javascript file, for example <em>experience.js</em></p>
    <p>Then, create a file named <em>experience.test.js</em> that will contain your test.</p>
    <p>After writing tests, you can run Jest like so:</p>
    <pre class="snippet">
      <code>
  $  yarn test
      </code>
    </pre>
    <h1>How to <span>run?</span></h1>
  </section>
`;

export default page4;
