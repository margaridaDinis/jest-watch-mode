const page8 = document.createElement('template');
page8.innerHTML = `
  <section>
    <p>If you run <b>a</b>, <b>o</b> will bring you back to the tests related to the files changed since last commit only.</p>
    <h1>Options <span class="respectCase">o</span></h1>
  </section>
`;

export default page8;
