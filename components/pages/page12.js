const page12 = document.createElement('template');
page12.innerHTML = `
  <section>
    <p>
      <b>Enter</b> runs the tests again with the filters and options previously set.
    </p>
    <h1>Options <span>Enter</span></h1>
  </section>
`;

export default page12;
